{
  "srcDir": "./src",
  "failOnError": true,
  "camelCaseColumnNames": false,
  "transforms": [
    {
      "mode": "sql",
      "include": "{routes,repositories}/**/types/*.typed.sql",
      "emitTemplate": "{{dir}}/{{name}}.queries.ts"
    }
  ]
}

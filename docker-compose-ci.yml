services:
  db:
    build: ./test/container-db
    command: postgres
      -c logging_collector=on
      -c log_destination=stderr
      -c log_filename=postgresql.log
      -c ssl=on
      -c ssl_cert_file=/etc/ssl/certs/ssl-cert-snakeoil.pem
      -c ssl_key_file=/etc/ssl/private/ssl-cert-snakeoil.key
    environment:
      # DOCS AT: https://hub.docker.com/_/postgres/
      - POSTGRES_DB=test_template
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=test
    ports:
      - '5430:5432'
    volumes:
      - ./test/container-db/schema:/docker-entrypoint-initdb.d/
  api:
    build:
      context: .
      args:
        - NPM_TOKEN
      dockerfile: test/Dockerfile.ci
    command: /srv/run.sh
    depends_on:
      - db
    entrypoint: /srv/wait-db-container.sh db 5432
    environment:
      - API_ENV=test
      - AWS_ACCESS_KEY_ID=${API_DEMO_AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${API_DEMO_AWS_SECRET_ACCESS_KEY}
      - NODE_ENV=production
    ports:
      - '6663:8000'
    tty: true
    volumes:
      - .:/srv/api
      - node-modules:/srv/api/node_modules/
  test:
    build: ./test/container-test
    command: /run.sh
    depends_on:
      - api
      - db
    entrypoint: /wait-api-container.sh api 8000
    environment:
      - PGDATABASE=test
      - PGUSER=test
      - PGPASSWORD=test
      - TEST_CASE
    links:
      - db
    tty: true 
    volumes:
     - ./test/container-test:/usr/app
    volumes_from:
      - api
volumes:
  node-modules:

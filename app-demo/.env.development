# Loaded by `npm run env:local` (default Vite dev mode).
# Targets the LOCAL api-demo (npm run api-up-sso) on :6662 — remote stage DB via AWS SSO.
# The API's CORS config already allow-lists http://localhost:5173.
VITE_API_URL=http://localhost:6662

# Sentry active locally, mirroring the API's LOCAL environment (full trace sampling). The DSN is a
# public client credential — safe to commit.
VITE_SENTRY_DSN=https://f744112239208b43d99641889a7ab822@o4510897005854720.ingest.us.sentry.io/4511691108122624
VITE_SENTRY_ENVIRONMENT=LOCAL

# Loaded by `npm run env:test` (Vite `--mode test`).
# Targets the TEST api-demo (npm run ci-up api) on :6663 — local Dockerized test DB.
# The API's CORS config already allow-lists http://localhost:5173.
VITE_API_URL=http://localhost:6663
